// import {reviewList} from "./ReviewList.js"
const product = object =>
{
    return `Name: ${object.name}, Description: ${object.description}, Price: ${object.price}, Quantity: ${object.quantity}`
}
export {product}